1.新建Django项目
	django-admin startproject mysite
	mysite下生成settings.py（配置文件）、urls.py（url映射）、wsgi(django server)
2.新建APP
	python manage.py startapp learn
	mysite/settings.py INSTALLED_APPS 添加 learn
	learn下生成admin.py,apps.py,models.py,tests.py,models.py
3.views, urls, template
	urls映射url与处理函数
	{% inclue 'base.html' %}
	{% block content %]
4.views与template
	render(request, 'base.html', {"key":"value"})
	{{key}}

	列表:
	{% for i in list %}
	{{i}}
	{% endfor %}
	
	
